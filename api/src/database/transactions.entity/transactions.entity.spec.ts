import { TransactionsEntity } from './transactions.entity';

describe('TransactionsEntity', () => {
    it('should be defined', () => {
        expect(new TransactionsEntity()).toBeDefined();
    });
});
