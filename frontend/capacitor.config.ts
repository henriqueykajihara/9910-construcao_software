import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'front-uniticket',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
